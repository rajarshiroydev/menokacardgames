# Project memory

Updated 2026-09-24. This repository file is durable agent memory; no external memory service is assumed.

- `docs/FEATURES.md` (added 2026-09-24 at the user's request) is the plain-language, non-technical list of current features. Keep it updated with every user-visible change; the user wants detailed behaviour recorded so it isn't lost.
- Read `docs/FEATURE-PLAN.md` before substantial work. It is the canonical living feature plan. Update decisions, status and future feature plans there.
- Multi-user implementation is authorized on `feature/multi-user-transition`, one reviewed step at a time. Stop and report after each step. Production rollout and historical ownership migration are not authorized yet.
- User clarified: one host account owns its own friend list and games; friends need no account. No groups, invitations or memberships in initial scope.
- Average session return including all rebuys is confirmed. Rank from the first eligible session with no provisional label; show the session count for context. This normalizes chip scale, not luck/opponents/session length.
- Current implementation branch is `feature/multi-user-transition`. Design is paused and preserved at `ui-sporty-glass-refresh`, commit `826142f`, unpushed as of this date.
- Do not push without explicit instruction. Keep local preview on port 3005 available during UI work.
- Preserve existing game rules/flows unless a requested feature changes them. Never seed production or infer old session ownership from names/stakes.
- No external persistent memory was written. Root AGENTS.md points future agents to this file and the plan.

- Follow-up: user explicitly approved average session return and host-owned guest lists. Prefer Neon Auth, verify project compatibility and confirm login methods when transitioning. Do not use a provisional label. Ask for historical session ownership rather than guessing. Ask other choices as they become relevant.
- Transition branch: `feature/multi-user-transition`. Step 1 installed project-scoped Next.js DevTools MCP in `.codex/config.toml`. Neon MCP is installed, authenticated and available.
- Step 2 uses Neon Managed Better Auth with email magic links only. Development is isolated on the Neon branch `multi-user-auth`; Magic Link is enabled there with five-minute expiry and new-user registration, and local `.env.local` targets that branch. Production Neon data/Auth and Vercel remain unchanged. The web page and poker APIs require a verified session, but owner-scoped data access is Step 3 and must be complete before deployment.
- Step 2 email authentication was verified end to end with the user's test address. Magic links return through `/auth/callback`, where the one-time verifier is exchanged server-side and secure session cookies are attached before redirecting to the ledger. Keep this callback covered by browser testing when the Neon Auth SDK changes.
- Account deletion lifecycle is confirmed for future implementation: requesting deletion immediately locks and hides the ledger and signs out/revokes access; the host can recover the account for 30 days; after the grace period, an automated purge permanently deletes the account and owned data. Provider backups expire under the documented provider schedule rather than being manually rewritten. The UI/privacy text must disclose that schedule before release.
- Step 3A inventory is in `docs/HISTORICAL-OWNERSHIP.md`: 28 sessions and 13 player records from the isolated Neon branch, grouped into 13 review cohorts. Ownership is only partially resolved; do not implement a backfill or infer the remaining ownership. Production was not queried or changed.
- Historical ownership review: cohorts A–G and J–L belong to Rajarshi; cohort M belongs to a separate Emon-led group; cohorts H–I remain unresolved. Rajarshi's confirmed friends are Debraj, Pratik, Rahul Basak, Ratan, Shubhankar, Soham, Utsav and Abhirup. The Emon-led group contains Emon, Abhirup and Supratik. Abhirup intentionally needs separate owner-local profiles in both ledgers. Aiush, Ashit and Rana are unresolved. Ratan has no historical database record. Never infer or migrate H–I or M until their ownership and target host accounts are confirmed.
- Step 3B added and rehearsed `migrations/0001_owner_scope_foundation.sql` only on the isolated Neon branch. It is expand-only and leaves all 13 players and 28 sessions unowned; no account or mapping data was inserted. Production remains unchanged. Do not remove legacy uniqueness, enable RLS, cut over routes or backfill history until their reviewed steps. Rehearsal evidence is in `docs/MIGRATION-REHEARSALS.md`.
- Step 3C added the authenticated account boundary. Server code provisions accounts only from a verified Neon Auth UUID and blocks non-active lifecycle states across every poker API. Browser verification created one active application account linked to the isolated Auth user; no player or session ownership changed. Queries are still global until the coordinated route-scoping step, so do not deploy this intermediate state.
- Step 3D applied `0002_owner_scoped_keys` on the isolated branch and scoped all player/session API operations to the verified account. Account-local duplicate names and client session IDs work; owner-local session numbering is atomic and retry safe. Cross-account SQL rehearsal passed and left no fixtures. The authenticated ledger is intentionally empty because all 13 legacy players and 28 legacy sessions remain unowned. Historical backfill is next. RLS and restricted runtime credentials are still required before deployment.
- Step 3E cloned the explicitly approved A–G and J–L cohorts into Rajarshi's isolated ledger: 9 friend profiles, 24 sessions and 72 results, with owner-local player IDs, display numbers 1–24 and provenance mappings. Reconciliation and idempotent rerun passed; Ratan has no fabricated history. The 13 legacy players and 28 legacy sessions remain intact, and H–I/M remain unowned. Production was not changed. Database-enforced isolation is next.
- Step 3F added transaction-local Neon Auth context, Postgres row-level security and the restricted `menoka_app` runtime role. A real two-account rehearsal passed cross-owner read, update, insert and pooled-connection reuse checks; the restricted browser still loads 9 players and 24 sessions. Neon API-created database roles inherit `neon_superuser`, so runtime roles must be SQL-created without administrative/BYPASSRLS capabilities. Production remains unchanged. Account-scoped browser storage and explicit legacy-draft handling are next.
- Step 3G keys active-game browser storage by internal account ID. Legacy global games/history are never auto-uploaded: unfinished games require explicit adoption, and legacy sessions require per-session review and selection with date, stakes and players visible. Unselected data stays unassigned on the device. The unused API-created rehearsal role was deleted with explicit approval. Production remains unchanged. Normalized session results/buy-ins and server-derived accounting are next.
- Step 3H added authoritative owner-bound `session_results` and `buy_in_events`. The isolated branch contains 24 normalized owned sessions, 72 results and 72 events with zero reconciliation mismatches; legacy JSON is compatibility-only. New saves validate whole-session accounting and write all records atomically. A restricted-role save/retry rehearsal passed and all fixtures were removed. The player-history FK is deferred so direct player deletion with history still fails at commit while whole-account cascades can complete. Runtime accounting grants are SELECT/INSERT only. Production remains unchanged. Average-session-return standings and graph are next.
- Step 3I replaced raw-chip standings with average session return (`lib/poker/standings.ts`, metric version 1). Eligibility reuses `deriveSessionAccounting`; ineligible results carry a reason and players without eligible sessions are unranked, never 0%. The default graph shows running average return; Raw Chips remains a toggle. Standings and graph show chips, not ₹. All supporting stats and the raw-chip graph use eligible sessions only; excluded sessions are listed on the row with a reason. A starting stack must be at least 1 (UI and server). User preference: choose the long-term, easy-to-debug option when trade-offs are delegated. Verified in the browser against 24 sessions and 8 ranked players. No database change.
- Step 3J removed the shared deletion password. Permanent player/session deletion now requires the owner's verified Neon Auth session to have been created within 10 minutes (`lib/auth/recent-sign-in.ts`, `requireRecentHostAccount`). Otherwise the API returns 403 `recent-sign-in-required` and the UI offers to email a fresh magic link. `DELETE_PASSWORD` is no longer read; remove it from Vercel at cutover. `npm run dev` now serves on port 3005. `/auth/callback` must strip existing session cookies before exchanging a verifier; otherwise the SDK returns the cached old session.
- The in-app browser pane keeps its own cookies. For browser verification, the user pastes a fresh magic link into the pane rather than clicking it in their mail client.
- Next.js was patched from 16.2.12 to 16.3.5 during Step 2 to resolve the current critical production dependency advisory. Keep framework and auth SDK versions pinned and rerun the full gate when they change.
